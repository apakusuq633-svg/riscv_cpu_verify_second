# 修复：

### 第一次对5级流水线做所有冲突修复（已基本完成所有指令测试）

1. 指令存储器由同步读改为组合读，解决 PC 与 instruction 错拍问题。//IF
![alt text](image.png)
2. PC 更新逻辑加入 stall 保持，避免 load-use 冒险时 PC 继续前进。//CPU_top
![alt text](image-1.png)
3. 寄存器堆读口改为组合读，并加入同周期写后读旁路。//IM
![alt text](image-2.png)
4. 修正 alu_control 对 addi 负立即数的误判问题，避免将 I 型负立即数误识别为 sub。//ALU_control
![alt text](image-3.png)
5. 扩展 data_mem 为字节寻址结构，支持 lb/lh/lbu/lhu/sb/sh/sw。//data_mem
    自行查阅
6. 验证分支和跳转时 IF_ID、ID_EX 的 flush 机制。//IF_ID,ID_EX
 ![alt text](image-4.png)
 ![alt text](image-5.png)